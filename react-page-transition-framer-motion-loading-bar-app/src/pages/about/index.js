import {Layout} from '../../layouts/default';
const AboutPage = ({setIsLoading}) => {
  return (
    <Layout setIsLoading={setIsLoading}>
      <div>{'About Page'}</div>
    </Layout>
  );
};

export {AboutPage};
