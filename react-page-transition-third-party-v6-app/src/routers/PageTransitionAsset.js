import {
  animations as packageAnimations,
  presets as packagePresets,
} from '@steveeeie/react-page-transition';

export const animations = Object.keys(packageAnimations);

export const presets = Object.keys(packagePresets);
